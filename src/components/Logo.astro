---
import { Image } from 'astro:assets'
import logo from '../assets/img/SEA2026-logo.png'

/**
 * Logo Component
 *
 * @description Logo description
 */
interface Props {
  /**
   * Whether to load the image eagerly or lazily
   * @default 'eager'
   */
  loading?: 'eager' | 'lazy'
}

const { loading = 'eager' }: Props = Astro.props
---

<a href="/" class="flex items-center gap-2 !no-underline">
  <Image src={logo} alt="SEA Logo" width="47" height="37" loading={loading} />
  <span class="font-bold">SEA 2026</span>
</a>

<style lang="scss">
  a {
    color: var(--foreground-color);
  }
</style>
